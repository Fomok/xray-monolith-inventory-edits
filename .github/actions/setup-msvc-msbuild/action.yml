runs:
  using: composite
  steps:
    - uses: './.github/actions/configure-pagefile'
    - uses: microsoft/setup-msbuild@v1.3.1
      with:
        msbuild-architecture: "x64"
    - uses: TheMrMilchmann/setup-msvc-dev@v3